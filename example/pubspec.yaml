name: blaink_flutter_example
description: Example app demonstrating Blaink Flutter SDK usage
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  blaink_flutter:
    path: ../
  firebase_messaging: ^14.7.10
  firebase_core: ^2.24.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true